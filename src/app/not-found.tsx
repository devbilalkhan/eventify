import Heading from "@/components/Heading";
import HeroContainer from "@/components/HeroContainer";

export default function NotFound() {
  return (
    <main>
      <HeroContainer>
        <Heading className="lg:text-4xl">Sorry, Page Not Found</Heading>
      </HeroContainer>
    </main>
  );
}
